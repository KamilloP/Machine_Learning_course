Command
```
jupyter nbconvert --to=html --template=classic FILE_NAME.ipynb
```
failed. I used online converter, however images from images folder have not loaded (other images loaded correctly).
HTML file is in folder html_folder.

I have run jupyter on colab.